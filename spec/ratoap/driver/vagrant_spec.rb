require 'spec_helper'

describe Ratoap::Driver::Vagrant do
  it 'has a version number' do
    expect(Ratoap::Driver::Vagrant::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
